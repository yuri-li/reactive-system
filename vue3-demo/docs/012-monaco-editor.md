# 1 简介

Monaco Editor 是一款开源的在线代码编辑器。它是 VSCode 的浏览器版本。

响应式编程，目前，前后端交互并不流畅，还需要开发一些工具：

- Swagger。使用RSocket开放的接口，不支持Swagger。

- json/json schema。后端定义好了接口，前端基础的CRUD，也可以自动生成。而使用kotlin data封装的model，还不能直接转换成前端可以使用的json，或json schema

这些工具都需要一些在线交互。

开始使用`Monaco Editor`开发项目`json-form`




